# Screenshots

Drop images here and uncomment the matching lines in the root `README.md`.

| Filename | What it should show |
|---|---|
| `dashboard.png` | The dashboard mid-run, ideally with several stages already green. |
| `pipeline.png` | The nine stages completed, with the trade line visible. |

Keep them under ~500 KB each; GitHub renders them inline and a heavy README is
slow to open on a phone, which is how most people will first see this.
